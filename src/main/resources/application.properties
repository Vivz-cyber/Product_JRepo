spring.application.name=Product_JRepo

server.port=${JPA_PORT:8082}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/tester_db?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:MySQL@123}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
